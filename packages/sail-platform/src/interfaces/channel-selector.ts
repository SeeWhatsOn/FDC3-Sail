/**
 * Channel Selector Interface
 *
 * Re-exported from @finos/sail-desktop-agent — desktop-agent owns the host contract.
 * ChannelSelector is retained as an alias of ChannelControl for Sail web compatibility.
 */

export type { ChannelControl, ChannelSelectionRequest } from "@finos/sail-desktop-agent"

export type { ChannelControl as ChannelSelector } from "@finos/sail-desktop-agent"
